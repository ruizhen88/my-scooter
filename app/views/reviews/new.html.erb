<%= simple_form_for [current_user, @booking, @review] do |f| %>
  <%= f.input :content, html5: true, class: "form-control" %>
  <%= f.input :rating, html5: true, class: "form-control" %>
  <%= f.submit "Post Review", class: "btn btn-primary" %>
<% end %>
